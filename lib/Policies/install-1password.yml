- name: macOS - 1Password installed
  query: SELECT 1 FROM apps WHERE bundle_identifier = 'com.brisk.Brisk';
  install_software:
    package_path: ../packages/brisk.yml
  critical: false
  description: Some great description goes here
  resolution: Brisk should be automatically installed. If it is missing, install it from self-service. 
  platform: darwin
